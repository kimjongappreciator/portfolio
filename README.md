# Portafolio

# ES
Este portafolio simula la ui de windows 98, fue hecho con react + vite + shadcn, ademas soporta ingles y espanol con una implementacion simple de localizacion custom

Para cambiar el idioma solo hace falta darle click a el EN o ES en la barra de tareas al lado del reloj

# EN

This portfolio simulates the Windows 98 UI. It was built using React + Vite + Shadcn, and supports both English and Spanish through a simple custom localization implementation.

To change the language, just click on "EN" or "ES" in the taskbar @ the left side of the clock.